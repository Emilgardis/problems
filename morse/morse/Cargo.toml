[package]
name = "morse"
version = "0.1.0"
authors = ["Emil Gardström <emil.gardstrom@gmail.com>"]

[dependencies]
serde = {version = "1.0", default-features = false }
serde_derive = "1.0"

[features]
default = ["std"]

std = ["serde/std"]

